name: Deployment to Render

on:
  push:
    branches:
      - main

jobs:
  deployment_to_render_pipeline:
    runs-on: ubuntu-20.04
    steps:
      - name: Trigger deployment
        run: curl https://api.render.com/deploy/srv-${{ secrets.RENDER_SERVICE_ID }}?key=${{ secrets.RENDER_API_KEY }}